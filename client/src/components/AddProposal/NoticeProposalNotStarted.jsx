function NoticeProposalNotStarted() {
  return (
    <div className="row align-items-center justify-content-center metamask-error">
      The proposal session has not started yet, come back later
    </div>
  );
}

export default NoticeProposalNotStarted;
